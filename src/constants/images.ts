export const HERO_PATH   = "/hero";
export const ACTION_PATH = "/action";
export const NFT_PATH    = "/nft-preview";
export const WP_PATH     = "/whitepaper";
export const HERO_IMAGES = Array.from({length:8},(_,i)=>`/hero/${i+1}.jpg`);
export const ACTION_IMAGES = Array.from({length:8},(_,i)=>`/action/${i+1}.jpg`);
export const NFT_IMAGES = Array.from({length:8},(_,i)=>`/nft-preview/${i+1}.jpg`);
