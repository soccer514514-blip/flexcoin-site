<<<<<<< HEAD
name: Build & Deploy (Vite + Pages)

=======
﻿name: Deploy Vite to GitHub Pages
>>>>>>> c157e31 (deploy: add hero images + vite base=/ + workflow + build)
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
<<<<<<< HEAD

=======
>>>>>>> c157e31 (deploy: add hero images + vite base=/ + workflow + build)
permissions:
  contents: read
  pages: write
  id-token: write
<<<<<<< HEAD

concurrency:
  group: "pages"
  cancel-in-progress: true

=======
concurrency:
  group: "pages"
  cancel-in-progress: true
>>>>>>> c157e31 (deploy: add hero images + vite base=/ + workflow + build)
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          # 🔓 락파일 추가 후 아래 두 줄 주석 해제하면 캐시 활성화 (빌드 더 빨라짐)
          # cache: npm
          # cache-dependency-path: package-lock.json

      - name: Install dependencies (ci if lockfile)
        shell: bash
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Build
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    runs-on: ubuntu-latest
    needs: build
=======
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist
  deploy:
    needs: build
    runs-on: ubuntu-latest
>>>>>>> c157e31 (deploy: add hero images + vite base=/ + workflow + build)
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
<<<<<<< HEAD
      - name: Deploy to GitHub Pages
        id: deployment
=======
      - id: deployment
>>>>>>> c157e31 (deploy: add hero images + vite base=/ + workflow + build)
        uses: actions/deploy-pages@v4
